#pragma once

enum layers {
	_BASE,
    _HRMOD,
    _GAMING,
	_LOWER,
    _LOWERFN,
	_NAV,
	_SYMBOL,
	_NUMPAD,
    _METALAYER
};


#define ROTARY_MOUSE_MODE_LAYER _SYMBOL
#define ROTARY_MOUSE_MODE_ADJUST _METALAYER
